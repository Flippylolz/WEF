name: CI Parser Smoke

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    name: Parser smoke
    runs-on: ubuntu-latest
    steps:
      - run: echo "workflow parser is healthy"
